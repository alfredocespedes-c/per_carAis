const $=(q)=>document.querySelector(q);function fmtDate(s){if(!s)return"-";return new Date(s).toLocaleString()}async function api(url,options={}){const r=await fetch(url,options);let body={};try{body=await r.json()}catch{}if(!r.ok)throw new Error(body.detail||"Error de servidor");return body}async function checkHealth(){try{const h=await api("/api/health");$("#health").textContent=`Servicio activo · umbral ${h.match_threshold}`}catch{$("#health").textContent="Servicio no disponible"}}$("#enrollForm").addEventListener("submit",async(e)=>{e.preventDefault();const box=$("#enrollResult");box.classList.remove("hidden");box.textContent="Procesando foto...";try{const res=await api("/api/people",{method:"POST",body:new FormData(e.target)});box.textContent=`${res.name} registrada correctamente.`;e.target.reset();await loadPeople()}catch(err){box.textContent=err.message}});$("#recognizeForm").addEventListener("submit",async(e)=>{e.preventDefault();const box=$("#recognitionResult");box.className="recognition empty";box.innerHTML='<div class="big-icon">⌁</div><strong>Analizando...</strong>';try{const res=await api("/api/recognize",{method:"POST",body:new FormData(e.target)});if(res.authorized){box.className="recognition allowed";box.innerHTML=`<div class="big-icon">✓</div><strong>ACCESO AUTORIZADO</strong><span>${res.person.name}</span><span>Similitud orientativa: ${res.similarity}% · distancia: ${res.distance}</span>`}else{box.className="recognition denied";box.innerHTML=`<div class="big-icon">×</div><strong>ACCESO DENEGADO</strong><span>Persona no reconocida sobre el umbral configurado.</span><span>Mejor candidato: ${res.best_candidate} · similitud orientativa: ${res.similarity}%</span>`}await loadLogs()}catch(err){box.className="recognition denied";box.innerHTML=`<div class="big-icon">!</div><strong>No se pudo analizar</strong><span>${err.message}</span>`}});async function loadPeople(){const rows=await api("/api/people");$("#peopleList").innerHTML=rows.length?`<table><thead><tr><th>Nombre</th><th>ID / RUT</th><th>Empresa</th><th>Estado</th><th>Registro</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${r.name}</strong></td><td>${r.external_id||"-"}</td><td>${r.company||"-"}</td><td><span class="badge ${r.active?"ok":"no"}">${r.active?"Activo":"Inactivo"}</span></td><td>${fmtDate(r.created_at)}</td></tr>`).join("")}</tbody></table>`:'<p class="muted">Todavía no hay personas registradas.</p>'}async function loadLogs(){const rows=await api("/api/logs");$("#logsList").innerHTML=rows.length?`<table><thead><tr><th>Fecha</th><th>Resultado</th><th>Persona</th><th>Similitud</th><th>Distancia</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${fmtDate(r.tested_at)}</td><td><span class="badge ${r.authorized?"ok":"no"}">${r.authorized?"Autorizado":"Denegado"}</span></td><td>${r.matched_name||"Desconocido"}</td><td>${Number(r.similarity).toFixed(1)}%</td><td>${Number(r.distance).toFixed(4)}</td></tr>`).join("")}</tbody></table>`:'<p class="muted">Todavía no hay pruebas realizadas.</p>'}checkHealth();loadPeople();loadLogs();
